#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>
#include <item.h>
#include <Pilha.h>

int main()
{
  int n;
  scanf("%d", &n);
  int i = 0;
  while (i < n)
  {
    char stringInput[1000];
    scanf(" %s", stringInput);
    i++;
  }
  return 0;
}
